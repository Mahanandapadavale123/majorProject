<% layout("/layout/boilerplate") %>
<body>
    <h3>All Listings</h3>
      <form action="/listings/new">
        <button>create new listing</button>
      </form>
    <ul>
        <%   for(let listing of allListings){ %>
             <li> <a href="/listings/><%=listing._id %>" <%= listing.title %> listings.title></a></li>
        <% }%>
     </ul>
</body>

